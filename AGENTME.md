# AGENTME：手勢控制系統協作規範

本文件給所有協作者與 AI Agent 使用，目標是讓專題維持「能跑、好懂、可展示」。

## 專題定位

這是一個以攝影機手勢控制餐點介面與 Windows 滑鼠的互動系統。核心重點是：

- 影像輸入穩定：攝影機讀取、鏡像與手部偵測要可靠。
- 手勢判斷清楚：手勢與動作對應要容易理解與調整。
- 前後端同步：Flask/Socket.IO 要正確把手勢結果送到網頁。
- 展示可用：修改後必須確認主程式至少能通過語法檢查。

## 修改規則

- 每次修改程式碼時，都要新增或保留合適的中文註解，說明該段邏輯的用途。
- 每次更新完成後，都要重新檢查專案架構，並同步整理 README，確保文件與實際程式一致。
- 只改與任務有關的檔案，不任意重構、不任意換框架、不任意移除既有功能。
- 優先做小而明確的優化，例如修正亂碼、修正語法錯誤、改善錯誤訊息、降低重複邏輯。
- 若修改手勢門檻、冷卻時間、滑鼠速度等參數，必須在註解或 README 中說明原因。
- 不提交大型模型檔、快取資料、虛擬環境或本機產生的暫存檔。

## 驗證規則

每次修改後至少執行：

```powershell
python -m compileall .
```

若修改手勢邏輯，還需要執行：

```powershell
python -m unittest discover -s tests
```

若修改 `main.py`、`flask_server/` 或前端檔案，還需要確認：

- `python main.py` 能啟動 Flask 與 OpenCV 視窗。
- 瀏覽器可開啟 `http://localhost:5000`。
- Focus Mode 與 Virtual Mouse Mode 的切換事件沒有報錯。

## 架構邊界

- `vision/`：只處理攝影機、手部偵測與手勢判斷。
- `control/`：只處理 Focus Mode 狀態或 Windows 滑鼠控制。
- `flask_server/`：只處理網頁、Socket.IO 事件與 UI 更新。
- `tests/`：只放自動化測試，不放展示素材或主程式邏輯。
- `main.py`：負責把上述模組串起來，不放過多細節邏輯。

## 交付標準

- README 的架構與實際檔案一致。
- Python 檔案沒有語法錯誤。
- 新增邏輯有中文註解。
- 每次更新後都有檢查並整理 README 與架構描述。
- 不破壞原本可展示的操作流程。
